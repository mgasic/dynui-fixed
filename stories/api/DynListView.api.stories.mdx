import type { Meta } from '@storybook/react-vite'
import { Title, Subtitle, Description, Primary, ArgsTable, Stories } from '@storybook/blocks'
import * as Types from '../../src/types/components/dyn-listview.types'

const meta: Meta = {
  title: 'Docs/API/DynListView',
  parameters: {
    docs: {
      page: () => (
        <>
          <Title />
          <Subtitle>API Reference</Subtitle>
          <Description>DynListView sa single/multi select, aria-multiselectable i role=listbox/option.</Description>
          <Primary />
          <ArgsTable of={Types} />
          <Stories />
        </>
      )
    }
  }
}
export default meta
