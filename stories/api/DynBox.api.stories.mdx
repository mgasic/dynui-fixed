import type { Meta } from '@storybook/react-vite'
import { Title, Subtitle, Description, Primary, ArgsTable, Stories } from '@storybook/blocks'
import * as Types from '../../src/types/components/dyn-box.types'

const meta: Meta = {
  title: 'Docs/API/DynBox',
  parameters: {
    docs: {
      page: () => (
        <>
          <Title />
          <Subtitle>API Reference</Subtitle>
          <Description>DynBox layout i spacing props (gap, p, m) uz CSS custom properties.</Description>
          <Primary />
          <ArgsTable of={Types} />
          <Stories />
        </>
      )
    }
  }
}
export default meta
