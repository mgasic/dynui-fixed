import type { Meta } from '@storybook/react'
import { Title, Subtitle, Description, Primary, ArgsTable, Stories } from '@storybook/blocks'
import * as Types from '../../src/types/components/dyn-menu.types'

const meta: Meta = {
  title: 'Docs/API/DynMenu',
  parameters: {
    docs: {
      page: () => (
        <>
          <Title />
          <Subtitle>API Reference</Subtitle>
          <Description>
            DynMenu now accepts <code>items</code> arrays of <code>DynMenuItemConfig</code> objects or
            custom <code>DynMenuItem</code> children for full control over layout, including dividers and
            keyboard interactions.
          </Description>
          <Primary />
          <ArgsTable of={Types} />
          <Stories />
        </>
      )
    }
  }
}
export default meta
