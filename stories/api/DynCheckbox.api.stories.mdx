import type { Meta } from '@storybook/react-vite'
import { Title, Subtitle, Description, Primary, ArgsTable, Stories } from '@storybook/blocks'
import * as Types from '../../src/types/components/dyn-checkbox.types'

const meta: Meta = {
  title: 'Docs/API/DynCheckbox',
  parameters: {
    docs: {
      page: () => (
        <>
          <Title />
          <Subtitle>API Reference</Subtitle>
          <Description>DynCheckbox props uključujući indeterminate stanje i a11y.</Description>
          <Primary />
          <ArgsTable of={Types} />
          <Stories />
        </>
      )
    }
  }
}
export default meta
